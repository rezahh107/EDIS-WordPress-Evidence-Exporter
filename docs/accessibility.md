# Accessibility

The admin UI uses semantic headings, fieldsets, labels, tables, progress elements, keyboard-operable controls, live regions, visible focus supplied by WordPress, RTL-aware layout, logical CSS properties, and reduced-motion handling. This is an implementation target, not an unsupported claim of formal conformance.
