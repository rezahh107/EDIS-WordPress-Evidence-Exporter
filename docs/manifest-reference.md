# Manifest Reference

The build manifest declares every generated file and every component. Components are classified as `SOURCE_COLLECTOR`, `INDEX_BUILDER`, or `BUNDLE_PROCESSOR`; dependencies are `REQUIRED`, `OPTIONAL`, or `CONDITIONAL`. No source scanning or runtime discovery is used to build the registry.
